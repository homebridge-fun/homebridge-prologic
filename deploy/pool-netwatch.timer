[Unit]
Description=Run the pad network watchdog every minute

[Timer]
OnBootSec=3min
OnUnitActiveSec=1min
# Don't stack catch-up runs after downtime; the next tick is enough.
Persistent=false

[Install]
WantedBy=timers.target
