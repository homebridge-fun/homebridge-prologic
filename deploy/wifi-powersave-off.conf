# Disable Wi-Fi power-save on the pad Pi. The Pi Zero 2 W (brcmfmac) drops off
# the network — and can appear wedged — when power-save idles the radio on a
# quiet, always-on link. 2 = disable. Installed to
# /etc/NetworkManager/conf.d/wifi-powersave-off.conf.
[connection]
wifi.powersave = 2
